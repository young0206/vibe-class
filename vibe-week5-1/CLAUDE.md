# CLAUDE.md

- HTML 구조는 `index.html`, 스타일은 `style.css`, 동작과 메뉴 데이터는 `script.js`에서 관리한다.
- 인라인 스타일을 사용하지 않는다.
- 새 메뉴는 `script.js`의 기존 `koreanMenus` 배열에만 추가한다.
- 기존 주황/크림 계열 디자인을 유지한다.
- 외부 라이브러리나 프레임워크를 추가하지 않는다.
- 수정 후 기존 메뉴 추천 기능을 브라우저에서 확인한다.

